# EMS Frontend

## Setup

1. Copy `.env.example` to `.env`
2. Install dependencies with `npm install`
3. Run `npm run dev`

Local development uses `/api` and Vite proxies requests to `http://localhost:5000`.
Set `VITE_API_URL` to your deployed backend API URL when deploying.
